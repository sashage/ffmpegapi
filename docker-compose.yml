services:
  app:
    build: .
    ports:
      - "${PORT:-3088}:${PORT:-3088}"
    # Note: Volume mount removed for production deployments
    # For local development with hot-reload, uncomment:
    # volumes:
    #   - .:/app
